services:
  app:
    build: .
    ports:
      # Bound to loopback only: the API has no authentication on any endpoint,
      # so publishing on all interfaces would expose it to the whole network.
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/bilianalyzer.db
    env_file:
      - .env
